If you want to install scrapy on Python 3, you’ll also need Python 3 development headers:

#sudo apt-get install python3 python3-dev

#pip install scrapy

Install requirement library

#pip install beautifulsoup4

Run scrapy spider
#scrapy crawl spain -o spain_20190902.csv

If you want to export json file, then run command like as the below
#scrapy crawl spain -o spain_20190902.json
